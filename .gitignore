# ============================================================
#  Remote_control - Git 忽略规则
#  仅学习研究用途，禁止用于任何非法用途（详见 README.md）
# ============================================================

# ---------- 1. 编译产物：不上传 ----------
# 所有构建脚本统一输出到仓库根目录 bin/
bin/
# 兼容：散落在外的编译产物也不入库
*.exe
*.dll
*.so
*.dylib
*.a
*.o
# 交叉编译生成（Linux 无扩展名可执行文件，如 helper_linux_amd64）
*_linux_amd64
*_linux_arm64

# ---------- 2. 日志：不上传 ----------
# 含 program.log / bots.log / botlogs/<botID>.log 等终端录像，属运行期本地数据
*.log
logs/
# 本地 CLI/Agent 接入点文件（含随机令牌），无论出现在哪都不入库
**/agent_endpoint.json

# ---------- 3. 本地配置与身份信息：不上传 ----------
# user 端首次运行在【运行目录】下自动生成 config.json，
# 内含协助者服务器地址、本机唯一身份 user_id、自定义名称 name，属本地私有信息
config.json
