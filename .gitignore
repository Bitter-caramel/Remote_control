# ============================================================
#  Remote_control - Git 忽略规则
#  仅学习研究用途，禁止用于任何非法用途（详见 README.md）
# ============================================================

# ---------- 1. 编译产物：不上传 ----------
# 由 生成exe.bat / 生成dll.bat 生成，属本机构建结果，不入库
*.exe
*.dll
*.so
*.dylib
*.a
*.o
# 由 生成linux.bat 交叉编译生成（Linux 无扩展名可执行文件，如 helper_linux_amd64）
*_linux_amd64
*_linux_arm64

# ---------- 2. 日志：不上传 ----------
# 含 program.log / bots.log / botlogs/<botID>.log 等终端录像，属运行期本地数据
*.log
logs/

# ---------- 3. 本地配置与身份信息：不上传 ----------
# user/config.json 由被控端首次运行自动生成，
# 内含协助者服务器地址与本机唯一身份 user_id，属本地私有信息
user/config.json
