# 面向对象

每个bot都是一个对象，为每个对象建立一个协程。协程是为了方便管理，协程之间（每个bot对象之间）不需要互相通信。

# 连接方式

在接收到请求之后，建立tcp连接，然后升级websocket长连接。通过反向shell的方式管理虚拟机、服务器、客户等等。

# 心跳检测

当一个周期内没有收到心跳的时候，将bot对象的指针写入缓存区列表，然后再等待，三个周期没有收到心跳之后，销毁协程，bots列表删除对应bot对象的指针，缓存区列表也删除指针，此时，机器下线。

# bots管理

主程序，有bots列表，保存每个bot的地址，便于后面对bot进行访问和管理。

bot运行程序之后，如果是第一次上线，会获取一个botID，保存到客户电脑中，后面会靠这个ID来识别用户。如果不是第一次上线，就读取本地的config文件，获取自己的botID，发起请求建立连接时，靠ID来识别用户。

对方下线程序之后，向我发送数据包，通知bot下线，然后程序销毁对应协程。bots列表删除存在列表中的指针。心跳消失也是如此。

# 日志

programlog：程序运行的日志，包括正确和错误日志。

botlog：每个bot建立连接，并注册进来之后，获得一个botID，用这个ID创建一个专属log文件，用于保存对应bot执行过的命令和对应回显。心跳无需写入log，心跳只用于检测连接是否存在。

botslog：用于记录所有连接过的主机，并写入对应的botlog的文件名，便于查找。

# 控制面板

保留一个前端的dos面板，就像codex的cli面板一样，用于接收既定的指令和参数。其余所有程序后台运行，运行过程生成日志。

#### 命令：

log -p：查看程序运行的日志。

log --bots:查看botslog日志。

log --botlog+ID：查看对应的botID

bots：查看已连接机器，输出每个bot的基础信息

bots -a：查看连接过的全部机器，程序结束运行之后，输出botslog。

bots -l：查看详细信息

bot [botID]：运行命令之后，会重开一个dos面板给这个bot，用于远程执行命令进行操作和管理，实现发送命令，然后接收回显。将输入的命令和执行的结果输出到对应的botlog日志中。

exit：退出程序，不再监听端口，不再执行服务程序，不再对外提供服务，直至下次启动程序。




