启动程序之后，通过提前写入协助者的服务器的IP和端口，主动向协助者的主机建立连接。
启动程序后，检测是否存在config文件。靠里面的userID识别身份。不存在就创建config文件，然后向协助者发起请求，获取userID，存在就直接读取config，将userID写入请求中。

在程序运行时，给予权限，让协助者可以执行命令，维护服务器，或者进行各种操作。具体权限需要用户自己选择，协助者只负责使用命令行进行协助，如果用户使用管理员权限运行，那么协助者就有管理员权限；如果是普通运行，那就只有普通权限。